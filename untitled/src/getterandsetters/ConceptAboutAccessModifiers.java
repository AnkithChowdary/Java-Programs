package getterandsetters;

public class ConceptAboutAccessModifiers {
  public static void main(String[] args) {
    // public can be accessed from anywhere
    //private can be accessed within the same class and subclasses
    //protected can be accessed can't be accessed from different package with no subclass(Subclass in the sense we we use extends keyword then we can access)
    //default can be accessed within the package 
  }
}
